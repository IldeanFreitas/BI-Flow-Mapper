# BI Flow Mapper — dev-only dependencies (test suite)
#
# NÃO faz parte da distribuição ao usuário final — isso fica em
# requirements.txt. Este arquivo é só para quem vai rodar/editar a suíte
# pytest em tests/backend.
#
# Install:
#   ./.venv/Scripts/python.exe -m pip install -r requirements-dev.txt
#
# Run:
#   ./.venv/Scripts/python.exe -m pytest tests/backend -v

pytest>=8.0,<10.0
pytest-cov>=6.0,<8.0
requests>=2.31,<3.0
ruff>=0.12,<1.0
pip-tools>=7.4,<8.0
